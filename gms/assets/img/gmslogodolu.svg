<?xml version="1.0" encoding="utf-8"?>
<!-- Generator: Adobe Illustrator 26.0.3, SVG Export Plug-In . SVG Version: 6.00 Build 0)  -->
<svg version="1.1" id="Layer_1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px"
	 viewBox="0 0 1746.2 372.7" style="enable-background:new 0 0 1746.2 372.7;" xml:space="preserve">

  <style>
    /* Style for INF (outline) - mimicking the original GMS style */
    .outline-text {
      font-family: sans-serif; /* Using a generic sans-serif, adjust if specific font is known */
      font-size: 500px; /* Approximate size */
      font-weight: 400; /* Light to normal weight */
      fill: white;
      stroke: white;
      stroke-width: 2.5; /* Stroke width for outline effect */
      stroke-linejoin: round;
      letter-spacing: 5px; /* Adjust letter spacing */
      /* The original has a double outline, which is hard to replicate perfectly with standard SVG stroke.
         This uses a single stroke as an approximation. */
    }

    /* Style for + (outline) - mimicking the original + style */
    .plus-sign {
      font-family: sans-serif;
      font-size: 600px;
      font-weight: 300; /* Consistent weight with outline */
      fill: white;
      stroke: white;
      stroke-width: 2.5;
      stroke-linejoin: round;
      /* Center the plus vertically if needed */
      dominant-baseline: central;
    }

    /* Style for LA (solid) - mimicking the original LA style */
    .solid-text {
      font-family: sans-serif; /* Might need a bolder variant like Arial Black or impact */
      font-size: 250px;
      font-weight: 400; /* Bolder weight */
      fill: white;
      stroke: none;
      letter-spacing: 0px; /* Adjust letter spacing for LAB */
    }
    
    /* Style for B (solid) - mimicking the original B style */
    .solid-textB {
      font-family: sans-serif; /* Might need a bolder variant like Arial Black or impact */
      font-size: 250px;
      font-weight: 400; /* Bolder weight */
      font-style: italic;
      fill: white;
      stroke: none;
      letter-spacing: 0px; /* Adjust letter spacing for LAB */
    }
  </style>

  <!-- Position the text elements -->
  <!-- Adjust X, Y coordinates and spacing carefully to match the original layout -->
  <!-- Using separate text elements for better positioning control -->
  <text x="60" y="365" class="outline-text">INF</text>
  <!-- Adjust X position for '+' based on INF width -->
  <text x="850" y="130" class="plus-sign">+</text><!-- Adjusted Y position for vertical centering -->
  <!-- Adjust X position for 'LAB' based on '+' position and width -->
  <text x="1210" y="255" class="solid-text">LA</text>
  <text x="1520" y="255" class="solid-textB">B</text>

</svg>